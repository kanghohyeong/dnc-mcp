<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DnC Jobs - Task Management</title>
  <link rel="stylesheet" href="/css/jira-style.css">
  <style>
    /* 페이지 레이아웃 */
    body {
      min-height: 100vh;
      padding: var(--spacing-xl) var(--spacing-lg);
    }

    /* 헤더 */
    .page-header {
      margin-bottom: var(--spacing-xl);
    }

    .page-title {
      margin-bottom: var(--spacing-sm);
    }

    .page-subtitle {
      color: var(--text-secondary);
      font-size: var(--font-size-md);
    }

    /* Job 목록 */
    .job-list {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }

    .job-card {
      background: var(--bg-primary);
      border-radius: var(--radius-md);
      border: 1px solid var(--border-color);
      padding: var(--spacing-lg);
      transition: all var(--transition-normal);
      cursor: pointer;
    }

    .job-card:hover {
      border-color: var(--color-primary);
      box-shadow: var(--shadow-md);
    }

    .job-card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-md);
    }

    .job-id {
      font-family: 'Courier New', monospace;
      font-size: var(--font-size-lg);
      font-weight: var(--font-weight-semibold);
      color: var(--color-primary);
    }

    .job-goal {
      color: var(--text-primary);
      line-height: 1.6;
      margin-bottom: var(--spacing-md);
    }

    .job-link {
      color: var(--color-primary);
      text-decoration: none;
      font-weight: var(--font-weight-medium);
      font-size: var(--font-size-sm);
      transition: color var(--transition-fast);
    }

    .job-link:hover {
      color: var(--color-primary-hover);
    }

    /* 빈 상태 */
    .empty-state {
      background: var(--bg-primary);
      border-radius: var(--radius-md);
      border: 2px dashed var(--border-color);
      padding: var(--spacing-xl) * 2;
      text-align: center;
    }

    .empty-state-icon {
      font-size: 48px;
      margin-bottom: var(--spacing-lg);
      opacity: 0.3;
    }

    .empty-state-title {
      font-size: var(--font-size-xl);
      font-weight: var(--font-weight-semibold);
      color: var(--text-secondary);
      margin-bottom: var(--spacing-sm);
    }

    .empty-state-description {
      color: var(--text-tertiary);
      font-size: var(--font-size-md);
    }

    /* 반응형 */
    @media (max-width: 768px) {
      body {
        padding: var(--spacing-lg) var(--spacing-md);
      }

      .job-card-header {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--spacing-sm);
      }

      .job-id {
        font-size: var(--font-size-md);
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="page-header">
      <h1 class="page-title">Task Management</h1>
      <p class="page-subtitle">DnC (Divide and Conquer) 작업 목록</p>
    </div>

    <% if (jobs.length===0) { %>
      <div class="empty-state fade-in">
        <div class="empty-state-icon">📋</div>
        <div class="empty-state-title">아직 생성된 작업이 없습니다</div>
        <div class="empty-state-description">새로운 작업을 생성하여 시작하세요</div>
      </div>
      <% } else { %>
        <div class="job-list fade-in" data-testid="job-list">
          <% jobs.forEach(job=> { %>
            <div class="job-card" onclick="location.href='/<%= job.id %>'" data-testid="job-item">
              <div class="job-card-header">
                <div class="job-id"><%= job.id %></div>
                <div class="status-badge status-<%= job.status %>" data-testid="job-status">
                  <%= job.status %>
                </div>
              </div>
              <div class="job-goal">
                <%= job.goal %>
              </div>
              <a href="/<%= job.id %>" class="job-link" onclick="event.stopPropagation()">
                상세 보기 →
              </a>
            </div>
            <% }) %>
        </div>
        <% } %>
  </div>
</body>

</html>
