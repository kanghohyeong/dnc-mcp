<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DnC Jobs</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      padding: 40px 20px;
    }

    .container {
      width: 100%;
      max-width: 1200px;
      animation: fadeIn 0.5s ease-in;
    }

    h1 {
      color: white;
      font-size: 2.5rem;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      margin-bottom: 30px;
      text-align: center;
    }

    .job-list {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .job-item {
      background: white;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      padding: 24px;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .job-item:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
    }

    .job-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .job-id {
      font-family: 'Courier New', monospace;
      color: #6366f1;
      font-weight: 600;
      font-size: 1.125rem;
    }

    .job-status {
      padding: 6px 12px;
      border-radius: 16px;
      font-size: 0.875rem;
      font-weight: 600;
      text-transform: uppercase;
    }

    .status-pending {
      background-color: #fef3c7;
      color: #92400e;
    }

    .status-in-progress {
      background-color: #dbeafe;
      color: #1e40af;
    }

    .status-done {
      background-color: #d1fae5;
      color: #065f46;
    }

    .job-goal {
      color: #374151;
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 16px;
    }

    .job-link {
      display: inline-block;
      padding: 10px 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      transition: opacity 0.2s ease;
    }

    .job-link:hover {
      opacity: 0.9;
    }

    .empty-state {
      background: white;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      padding: 60px 40px;
      text-align: center;
      color: #9ca3af;
      font-style: italic;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 1.75rem;
      }

      .job-item {
        padding: 16px;
      }

      .job-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 8px;
      }

      .job-id {
        font-size: 1rem;
      }

      .job-goal {
        font-size: 0.875rem;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>DnC Jobs</h1>

    <% if (jobs.length===0) { %>
      <div class="empty-state">
        <p>아직 생성된 job이 없습니다.</p>
        <p style="margin-top: 8px;">/dnc-new 명령으로 새로운 job을 생성하세요.</p>
      </div>
      <% } else { %>
        <div class="job-list">
          <% jobs.forEach(job=> { %>
            <div class="job-item" data-testid="job-item">
              <div class="job-header">
                <div class="job-id">
                  <%= job.id %>
                </div>
                <div class="job-status status-<%= job.status %>" data-testid="job-status">
                  <%= job.status %>
                </div>
              </div>
              <div class="job-goal">
                <%= job.goal %>
              </div>
              <a href="/dnc/jobs/<%= job.id %>" class="job-link">
                상세 보기 →
              </a>
            </div>
            <% }) %>
        </div>
        <% } %>
  </div>
</body>

</html>
